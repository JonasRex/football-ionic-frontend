import { IonContent, IonTitle } from '@ionic/react';

const TransferTab: React.FC = () => {
  return (
    <IonContent color={'white-background'}>
      <IonTitle>TransferTab</IonTitle>
    </IonContent>
  );
};

export default TransferTab;
