import { IonContent, IonTitle } from '@ionic/react';

const MainTab: React.FC = () => {
  return (
    <IonContent color={'white-background'}>
      <IonTitle>MainTab</IonTitle>
    </IonContent>
  );
};

export default MainTab;
