import { IonContent, IonTitle } from '@ionic/react';

const WorldTab: React.FC = () => {
  return (
    <IonContent color={'white-background'}>
      <IonTitle>WorldTab</IonTitle>
    </IonContent>
  );
};

export default WorldTab;
